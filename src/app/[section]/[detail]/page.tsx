import { notFound } from "next/navigation"; import { SiteHeader } from "@/components/site-header"; import { SiteFooter } from "@/components/site-footer";
const allowed:Record<string,string[]>={platform:['overview','workflow','automation','reporting','analytics'],solutions:['hospital-laboratory','clinical-laboratory','pathology','microbiology','blood-bank','nova-ai'],modules:['routine-hematology','blood-bank','inventory-reagent','pathology','microbiology'],'nova-ai':['how-it-works','ai-doctor'],integration:['simrs-his','rme','analyzer','satusehat','connectivity']};
export default async function Detail({params}:{params:Promise<{section:string,detail:string}>}){const {section,detail}=await params;if(!allowed[section]?.includes(detail))notFound();const name=detail.split('-').map(x=>x[0].toUpperCase()+x.slice(1)).join(' ');return <><SiteHeader/><main className="grid-bg"><section className="container section min-h-[60vh] py-28"><p className="eyebrow">DIAGNOVA / {section}</p><h1 className="mt-5 text-5xl font-semibold md:text-7xl">{name}</h1><p className="mt-7 max-w-2xl text-lg leading-8 text-slate-600">Content for this Diagnova capability is being prepared with the product team to ensure it accurately reflects its clinical and operational scope.</p>{section==='nova-ai'&&<p className="mt-10 border-l-2 border-[#56ADE2] pl-5 font-semibold">AI assists. Doctors decide.</p>}</section></main><SiteFooter/></>}
